#include "holberton.h"
#include <stdio.h>

/**
 * print_diagsums - prints the sum of two diagonals in a square matrix
 * @a: pointer to array
 * @size: size of array
 */

void print_diagsums(int *a, int size)
{
	int i, j;
	int s1 = 0;
/*	int s2 = 0; */

	while (i  <= size * size)
	{
		s1 = *(a + s1);
		(a + (i + size + j);
	}
	printf("%d\n",s1);
}
