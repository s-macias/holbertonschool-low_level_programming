#include "holberton.h"
/**
 * 0-isupper checks if a character is uppercase 
* return (1) - if c is uppercase
 * return (0) - if otherwise
 * @c - the character to check for case

int _isupper(int c)
{
if (c
}
