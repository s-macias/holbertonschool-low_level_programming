#include "holberton.h"

/**
 * print_times_table - prints the n-times table, starting with 0
 * @n: number whose times table we need to print
 *
 * Return: nothing
 */

void print_times_table(int n)
{
	int m1, m2, m;

	if (n < 0  && n > 15)
		return;
}
